Tier: normal    Branch: unit/w5    Spec: prompts/sonnet-w5-server-backend-tests.md
Read AGENTS.md at the repo root first, then the spec file, then only the files the spec lists.

Task:
Unit W5 in repo embarazo.2.1: apply the backend-interface pattern from lib/server/sync.ts plus sync.test.ts and lib/server/sharing.ts plus sharingBackend.ts plus sharing.test.ts (V2, merged) to questions.ts, support.ts, push.ts and photos.ts, per the spec file. Rules stay in the module, storage moves behind an interface, the real rules run in memory in vitest. Behaviour must not change; the existing routeContract text tests stay and keep passing.
Work on branch unit/w5 created from the current main. Commit there.

Files to touch:
lib/server/questions.ts, support.ts, push.ts, photos.ts
their new *Backend.ts and *.test.ts siblings
their app/api/v1/** routes (wiring only)
docs/log/w5.md

Do not touch:
DECISIONS.md, docs/BUILD-PLAN.md, KNOWN-ISSUES.md, lib/db.ts, lib/auth/**, next.config.ts, .env*, package-lock.json except via npm, anything under public/assets/ not listed above. lib/server/sharing.ts and sync.ts (the exemplars; read, do not edit). Any zod schema.

Definition of done:
Every exit criterion in the spec file.
Each of the four modules has a behavioural test file exercising at least: the happy path, the forbidden-actor path, and the not-found path, against an in-memory backend.
npm test count rises and the routeContract tests are untouched and green.
git diff on the four route files shows only import and construction changes.
Stay within the listed files. If something outside them must change to satisfy the definition of done, stop and say so instead of changing it.
Do not push, open a PR, or merge. Commit on the branch named above and stop.

Commands to run before reporting:
npx tsc --noEmit   (working directory: repo root)
npm run lint   (working directory: repo root)
npm test   (working directory: repo root)
npm run test:db   (working directory: repo root)
npm run validate:content   (working directory: repo root)
npm run build with env PHOTO_STORAGE_ENDPOINT=https://bucket.example.test and NEXT_PUBLIC_SUPPORT_EMAIL=hola@mibebe.example.py   (working directory: repo root)
npx playwright test e2e/preguntas.spec.ts e2e/admin.spec.ts e2e/photo-backup.spec.ts e2e/companion.spec.ts   (working directory: repo root)
Run every listed command. Report FAIL with the error text rather than skipping or substituting another check.

Report format:
Files changed: each path with a one-line summary of the change.
Commands run: each command with PASS or FAIL and a one-line result.
Flagged or not done: anything skipped, blocked, ambiguous, or outside scope, or None.
Keep the report under 30 lines. No diffs, no file dumps. Never print or write secrets, tokens, or API keys.
