Tier: normal    Branch: unit/w2    Spec: prompts/sonnet-w2-docs-index.md
Read AGENTS.md at the repo root first, then the spec file, then only the files the spec lists.

Task:
Unit W2 in repo embarazo.2.1: docs/INDEX.md as the single entrance to docs/, move superseded plans under docs/archive/ with their banners intact, add a README pointer line, and extend tsconfig include so scripts/*.mts are typechecked, fixing the two known type errors in scripts/gen-guarani-review.mts, per the spec file. Add docs/PLAN-2026-09-13-GO-LIVE.md to the index as current.
Work on branch unit/w2 created from the current main. Commit there.

Files to touch:
docs/INDEX.md
docs/archive/**
README.md (one pointer line)
tsconfig.json (include only)
scripts/gen-guarani-review.mts
docs/log/w2.md

Do not touch:
DECISIONS.md, docs/BUILD-PLAN.md, KNOWN-ISSUES.md, lib/db.ts, lib/auth/**, next.config.ts, .env*, package-lock.json except via npm, anything under public/assets/ not listed above. The content of any moved document; git mv only.

Definition of done:
Every exit criterion in the spec file.
npx tsc --noEmit now covers scripts/*.mts and is clean.
npm run gen:guarani-review produces an unchanged docs/GUARANI-REVIEW.md (git diff empty on that file).
Every file under docs/ appears in INDEX.md exactly once, marked current or history.
Stay within the listed files. If something outside them must change to satisfy the definition of done, stop and say so instead of changing it.
Do not push, open a PR, or merge. Commit on the branch named above and stop.

Commands to run before reporting:
npx tsc --noEmit   (working directory: repo root)
npm run lint   (working directory: repo root)
npm test   (working directory: repo root)
npm run test:db   (working directory: repo root)
npm run validate:content   (working directory: repo root)
npm run build with env PHOTO_STORAGE_ENDPOINT=https://bucket.example.test and NEXT_PUBLIC_SUPPORT_EMAIL=hola@mibebe.example.py   (working directory: repo root)
npm run gen:guarani-review   (working directory: repo root)
Run every listed command. Report FAIL with the error text rather than skipping or substituting another check.

Report format:
Files changed: each path with a one-line summary of the change.
Commands run: each command with PASS or FAIL and a one-line result.
Flagged or not done: anything skipped, blocked, ambiguous, or outside scope, or None.
Keep the report under 30 lines. No diffs, no file dumps. Never print or write secrets, tokens, or API keys.
