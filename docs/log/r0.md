# R0 — Codex-queued review pass, run by Sonnet

Spec: `prompts/codex/r0-review.txt`. Run 2026-09-16, read-only, no files
changed. Full findings are in the session transcript; this log records the
triage per `docs/PLAN-2026-09-13-GO-LIVE.md` §2.6.

## Accepted, dispatched as units

| Finding | Severity | Unit | Files |
|---|---|---|---|
| `toISOString().slice(0,10)` computes tomorrow's date in the evening (Asunción UTC-3), so `addPeriod()` silently no-ops with no user feedback; same bug at 4 more sites, one of them (weight entry) with no future-date guard at all | P0 | **R0-1** | `app/(app)/planeando/calendario/page.tsx`, `components/Onboarding.tsx`, `app/(app)/ajustes/AjustesClient.tsx`, `app/(app)/herramientas/peso/page.tsx` — all switched to the existing `toDateInput()` helper (`lib/appointments.ts`, which already documents this exact trap) |
| `/emergencia` and `/derechos` are documented as offline-precached but absent from the service worker's `pageRoutes` list — a cold offline-first-open misses the safety page | P0 | **R0-2** | `app/sw.ts` |
| Email+password sign-in/registration has no rate limiting, unlike every other route in the app | P0 | **R0-3** | `app/(app)/cuenta/actions.ts`, `lib/server/auth.ts` |
| PR-20 password auth flow has zero test coverage | P1 | folded into **R0-3** | same files, same PR |
| `/api/v1/go/[id]/route.ts` rate-limit key has no namespace prefix, unlike every other route | P2 | folded into **R0-3** | `app/api/v1/go/[id]/route.ts` (one-line fix, low risk to bundle) |

## Verified clean (not re-checked)

Pregnancy/cycle math, sync engine (`lib/sync/**`, `lib/server/sync.ts`),
Dexie schema/upgrades, family-sharing authorization, photo backup route,
CSP, service worker update flow, admin panel, API routes that must 404
without their env vars, `lib/crypto.ts` PIN encryption. See the full report
in-session for verification detail on each.

## Flagged, not re-verified here (already queued elsewhere)

- Server-side behavior of `lib/server/support.ts`, `questions.ts`,
  `push.ts`, `photos.ts` beyond source reading — this is unit **W5** in the
  go-live plan.
- Live MySQL behavior of `drizzleBackend.upsertMany`'s
  `ON DUPLICATE KEY UPDATE` — no DB available read-only; in-memory
  `SyncBackend` tests cover the same rules, not the raw SQL.
- Real Lighthouse/mobile timing — this is unit **W6**.
