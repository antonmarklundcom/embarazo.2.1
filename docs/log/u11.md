# U11 — link pass

**Built**
- `app/(app)/page.tsx`: mounts `RecomendadosRail` (U3) with `trimester={trimester}`,
  between `HomeToolsGrid` and `WeekArticleFeed`. Self-gates on `useFlag("recomendados")`
  as built; no call-site conditional added.
- `app/(app)/ajustes/AjustesClient.tsx`: a new "Fondo de la semana" `SettingsGroup` right
  after "Idioma · Ñe'ẽ", a second door into U7's `ThemeSheet` (the first is the hero's
  `ThemeChip`). Reads/writes only through `lib/hero/preferences.ts` — no second storage.
- `app/admin/page.tsx`: an AI-spend alert banner (reusing `aiSpendReport`/`aiBabyAlertShare`
  as-is, fails gracefully with no database) plus two quick-link cards to `/admin/flags` and
  `/admin/ia`.
- `e2e/home.spec.ts` (new): Recomendados rail present/absent in both flag states on the
  home screen, mirroring `e2e/recomendados.spec.ts`'s mocking pattern.
- `DECISIONS.md`, `docs/BUILD-PLAN.md`, `docs/BUILD-QUEUE-2026-09-06.md` §10,
  `docs/HANDOFF-2026-09-06.md` §5, `KNOWN-ISSUES.md` (new) — per the brief.
- Verified every `HomeToolsGrid` tile and every `app/admin/layout.tsx` nav route resolves
  to a real `page.tsx`; no broken links found.

**Decisions**
- Worktree started from a stale base missing U3/U8 (both had merged to `origin/main` as
  real PRs #101/#108 mid-session); rebuilt `unit/u11` cleanly from current `origin/main`
  rather than hand-merging branches, per the manager's direction.
- `/admin`'s quick links are plain inline `<Link>` cards, not a new component — the nav bar
  already covers the same routes; this is a second, more discoverable entry point only.
- The AI-spend banner reads only the *current* month's `MonthSpend` (index 0 of
  `aiSpendReport`'s result) — the previous month is already closed and cannot be paused,
  so alerting on it would have no action to take.
- `useHeroTheme()`/`heroTheme(id).label` in ajustes shows the current theme's name on the
  row itself, a small addition beyond the brief's minimum, reusing existing exports rather
  than adding new UI state.

**Known issues**
- The one still-open item in `docs/decisions-needed.md` ("for the link pass") is the W4
  PIN-card extraction — `lib/pinPolicy.test.ts` (K18) reads `AjustesClient.tsx` by path, and
  the fix touches `lib/**` and `components/ajustes/PinSettings.tsx`, both outside this
  unit's Owns block. Left untouched, repeated verbatim in the closing report.
- See `KNOWN-ISSUES.md` for every other still-open item promoted from earlier units' logs.

**Verification** — see the closing report for full command output and exact counts.
